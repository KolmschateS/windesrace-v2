<Window x:Class="WPFApp.RaceStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d"
        Title="RaceStatistics" Height="450" Width="800">
    <Window.DataContext>
        <local:RaceStatisticsDataContext/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" FontWeight="Bold" FontSize="16">Track:</Label>
            <Label Grid.Column="1" FontSize="16" Content="{Binding CurrentRace.Track.Name}"></Label>
        </Grid>
        <Grid Grid.Row="2">
            <ListView ItemsSource="{Binding Participants}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
                        <GridViewColumn Header="Team" DisplayMemberBinding="{Binding TeamColor}"></GridViewColumn>
                        <GridViewColumn Header="Speed" DisplayMemberBinding="{Binding Equipment.Speed}" />
                        <GridViewColumn Header="Performance" DisplayMemberBinding="{Binding Equipment.Performance}" />
                        <GridViewColumn Header="Quality" DisplayMemberBinding="{Binding Equipment.Quality}" />
                        <GridViewColumn Header="IsBroken" DisplayMemberBinding="{Binding Equipment.IsBroken}"></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
